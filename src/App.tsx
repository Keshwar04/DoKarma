/**
 * @DoKarma App Component
 *
 * This is the root component of the application.
 * It renders the main router component (Routers) which handles all the routing logic.
 **/
import Routers from "./router/route";
const App = () => <Routers />;

export default App;
